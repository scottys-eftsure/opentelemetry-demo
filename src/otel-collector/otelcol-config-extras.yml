# Copyright The OpenTelemetry Authors
# SPDX-License-Identifier: Apache-2.0

# Extras/customization layer for the OTel Collector.
# Override this file in your fork to add exporters for your own observability backend.
#
# This file is intentionally empty. It is always loaded last, giving forks
# a stable, well-defined seam to customize without touching upstream files.
#
# IMPORTANT: The OTel Collector merges config files but REPLACES arrays, not appends.
# If you want to keep the default observability stack alongside your own exporters,
# you must explicitly repeat the upstream exporter names in your pipeline definitions.
#
# Upstream exporter names (repeat these to keep them active):
#   traces:  otlp_grpc/jaeger, debug, span_metrics
#   metrics: otlp_http/prometheus, debug
#   logs:    opensearch, debug
#   profiles: otlp_grpc/firepit, debug

# Grafana Cloud OTLP exporter integration
# Credentials are sourced from environment variables defined in .env.override

extensions:
  basicauth/grafana_cloud:
    # https://github.com/open-telemetry/opentelemetry-collector-contrib/tree/main/extension/basicauthextension
    client_auth:
      username: "${env:GRAFANA_CLOUD_INSTANCE_ID}"
      password: "${env:GRAFANA_CLOUD_API_KEY}"

processors:
  # Add static Grafana host ID to all telemetry resources
  # Required for Grafana Application Observability.
  # Also hard-overrides the namespace to "ctf" on every signal, and attributes
  # docker_stats container metrics to a service instance (see metric_statements).
  transform/add_host_id:
    error_mode: ignore
    trace_statements:
      - context: resource
        statements:
          - set(resource.attributes["grafana.host.id"], "a7f8e4c2-3d1b-4a5c-9f2e-6b8d7c4a1e9f") where resource.attributes["grafana.host.id"] == nil
          # Hard-override the namespace for all telemetry
          - set(resource.attributes["service.namespace"], "ctf")
    metric_statements:
      - context: resource
        statements:
          - set(resource.attributes["grafana.host.id"], "a7f8e4c2-3d1b-4a5c-9f2e-6b8d7c4a1e9f") where resource.attributes["grafana.host.id"] == nil
          # Attribute docker_stats container metrics to a service instance so their
          # full resource set (CPU, memory, network, block I/O) rolls up under a
          # ServiceInstance in the Knowledge Graph. Identified by having a
          # container.name but no service.name yet. The instance id is the unique
          # per-container container.id GUID.
          - set(resource.attributes["service.name"], resource.attributes["container.name"]) where resource.attributes["service.name"] == nil and resource.attributes["container.name"] != nil
          - set(resource.attributes["service"], resource.attributes["service.name"]) where resource.attributes["service"] == nil and resource.attributes["service.name"] != nil

          - set(resource.attributes["service.instance.id"], resource.attributes["container.id"]) where resource.attributes["service.instance.id"] == nil and resource.attributes["container.id"] != nil
          # Hard-override the namespace for all telemetry
          - set(resource.attributes["service.namespace"], "ctf")
          
          - set(resource.attributes["namespace"], "ctf")
    log_statements:
      - context: resource
        statements:
          - set(resource.attributes["grafana.host.id"], "a7f8e4c2-3d1b-4a5c-9f2e-6b8d7c4a1e9f") where resource.attributes["grafana.host.id"] == nil
          # Hard-override the namespace for all telemetry
          - set(resource.attributes["service.namespace"], "ctf")
    profile_statements:
      - context: resource
        statements:
          - set(resource.attributes["grafana.host.id"], "a7f8e4c2-3d1b-4a5c-9f2e-6b8d7c4a1e9f") where resource.attributes["grafana.host.id"] == nil
          # Hard-override the namespace for all telemetry
          - set(resource.attributes["service.namespace"], "ctf")

  # Batch telemetry before export to Grafana Cloud. Without this the trace
  # pipeline sends many tiny requests; under load (and with EU-endpoint latency)
  # the exporter queue backs up and drops spans. Batching cuts request count and
  # lets the queue drain. memory_limiter must run before batch (see pipelines).
  batch:
    send_batch_size: 8192
    send_batch_max_size: 16384
    timeout: 2s

  # Filter to drop ALL load-generator telemetry before sending to Grafana Cloud
  # Used for CTF exercises to prevent load-generator noise from muddying investigation data
  filter/drop_loadgen:
    error_mode: ignore
    metric_conditions:
      - resource.attributes["service.name"] == "load-generator"
    trace_conditions:
      - resource.attributes["service.name"] == "load-generator"
    log_conditions:
      - resource.attributes["service.name"] == "load-generator"
    profile_conditions:
      - resource.attributes["service.name"] == "load-generator"

  # Stamp the labels Grafana's entity Metrics view filters on (service, namespace,
  # asserts_env, asserts_site) as DATAPOINT attributes. OTLP->Prometheus promotes
  # datapoint attributes to per-series labels, whereas resource attributes only land
  # on target_info -- so these must be datapoint-level to appear on each metric.
  # This runs at the END of the metrics pipeline (after resourcedetection), so
  # deployment.environment.name and cloud.region are already on the resource and
  # asserts_env/asserts_site can be read dynamically rather than hard-coded.
  transform/asserts_labels:
    error_mode: ignore
    metric_statements:
      - context: datapoint
        statements:
          - set(attributes["service"], resource.attributes["service.name"]) where resource.attributes["service.name"] != nil
          - set(attributes["namespace"], resource.attributes["service.namespace"]) where resource.attributes["service.namespace"] != nil
          - set(attributes["asserts_env"], resource.attributes["deployment.environment.name"]) where resource.attributes["deployment.environment.name"] != nil
          - set(attributes["asserts_site"], resource.attributes["cloud.region"]) where resource.attributes["cloud.region"] != nil

  # Fix mis-scaled docker_stats "percent" metrics. The receiver emits container.memory.percent
  # and container.cpu.utilization as 0-100 values but tags them unit "1" (a 0-1 ratio), so the
  # Prometheus exporter names them *_ratio and Grafana renders them x100 (e.g. 45 -> 4500%).
  # Divide by 100 here so they become genuine 0-1 ratios; every App O11y panel then shows the
  # correct percentage, and multi-core CPU (>1 core) correctly reads >100%.
  transform/fix_container_pct_scale:
    error_mode: ignore
    metric_statements:
      - context: datapoint
        statements:
          - set(value_double, value_double / 100) where metric.name == "container.memory.percent"
          - set(value_double, value_double / 100) where metric.name == "container.cpu.utilization"

  # CTF metric challenges: stamp an `incident_token` datapoint attribute (promoted to a
  # per-series Prometheus label) onto a service's resource metrics when they cross a severity
  # threshold -- i.e. the token only appears once the symptom is bad enough, like an alert firing.
  # NOTE: names here are the in-pipeline OTel (dotted) names, NOT the Prometheus names you see in
  # Grafana (container.memory.percent -> container_memory_percent_ratio, etc). Two metrics carry
  # each token for redundancy (in case Adaptive Metrics drops a label or players look elsewhere).
  # Thresholds: email = safe separation; ad CPU = CALIBRATE against the live spike (see comments).
  transform/ctf_metric_flags:
    error_mode: ignore
    metric_statements:
      - context: datapoint
        statements:
          # Runs AFTER transform/fix_container_pct_scale, so container.* values are now 0-1 ratios.
          # #4 emailMemoryLeak -- baseline ~0.11 ratio / ~57MB, leak -> ~0.5 ratio / ~256MB+
          - set(attributes["incident_token"], "TESTING_FLAG{email_heap_creep_v5t8r}") where metric.name == "container.memory.percent" and resource.attributes["service.name"] == "email" and value_double > 0.4
          - set(attributes["incident_token"], "TESTING_FLAG{email_heap_creep_v5t8r}") where metric.name == "container.memory.usage.total" and resource.attributes["service.name"] == "email" and value_int > 150000000
          # #3 adHighCpu -- ad is CPU-capped to 2 cores (compose.extras.yaml). The 4 CPULoad threads
          # over-subscribe it 2:1, so the JVM saturates and ad starves (throttling + request errors):
          # jvm.cpu.recent_utilization (cgroup-aware) pegs to ~1.0 vs a bursty baseline <=~0.5, so the
          # threshold is 0.7. container.cpu.utilization stays host-relative (~0.057) so that rule
          # rarely fires -- kept as best-effort redundancy; the JVM gauge is the real signal.
          - set(attributes["incident_token"], "TESTING_FLAG{ad_cpu_thermal_q9w3e}") where metric.name == "jvm.cpu.recent_utilization" and resource.attributes["service.name"] == "ad" and value_double > 0.7
          - set(attributes["incident_token"], "TESTING_FLAG{ad_cpu_thermal_q9w3e}") where metric.name == "container.cpu.utilization" and resource.attributes["service.name"] == "ad" and value_double > 0.18

  # flagd EventStream is a long-lived gRPC subscription; its client spans end with an
  # error status on normal stream cancel/reconnect, which inflates the span-metrics
  # error ratio and trips KG ErrorRatioBreach across every service that talks to flagd.
  # Reclassify these benign cases (the EventStream method, plus transient CANCELLED(1)/
  # UNAVAILABLE(14) connection-cycle codes) to UNSET so they aren't counted as errors.
  # Genuine flag-eval failures (any other status code) are left untouched.
  transform/suppress_flagd_stream_noise:
    error_mode: ignore
    trace_statements:
      - context: span
        statements:
          - set(status.code, STATUS_CODE_UNSET) where status.code == STATUS_CODE_ERROR and attributes["rpc.service"] == "flagd.evaluation.v2.Service" and attributes["rpc.method"] == "EventStream"
          - set(status.code, STATUS_CODE_UNSET) where status.code == STATUS_CODE_ERROR and attributes["rpc.service"] == "flagd.evaluation.v2.Service" and (attributes["rpc.grpc.status_code"] == 1 or attributes["rpc.grpc.status_code"] == 14)

exporters:
  otlphttp/grafana_cloud:
    # https://github.com/open-telemetry/opentelemetry-collector/tree/main/exporter/otlphttpexporter
    endpoint: "${env:GRAFANA_CLOUD_OTLP_ENDPOINT}"
    auth:
      authenticator: basicauth/grafana_cloud
    # Larger queue + more parallel senders so a transient gateway 502 or the
    # round-trip latency to the EU endpoint doesn't fill the queue and drop data.
    sending_queue:
      enabled: true
      num_consumers: 20
      queue_size: 20000
    retry_on_failure:
      enabled: true
      initial_interval: 1s
      max_interval: 30s
      max_elapsed_time: 120s

connectors:
  grafanacloud:
    host_identifiers: ["host.id"]
    metrics_flush_interval: 60s

service:
  extensions: [basicauth/grafana_cloud]
  pipelines:
    traces:
      # Add host ID first, then apply resource detection and other processors; batch last before export
      processors: [transform/add_host_id, resourcedetection, memory_limiter, transform/sanitize_spans, transform/suppress_flagd_stream_noise, filter/drop_loadgen, batch]
      exporters: [debug, span_metrics, otlphttp/grafana_cloud]
    metrics:
      # Add host ID first, then resource detection; stamp asserts/service labels after
      # detection (so env/region are present), then batch last before export.
      processors: [transform/add_host_id, resourcedetection, memory_limiter, filter/drop_loadgen, transform/asserts_labels, transform/fix_container_pct_scale, transform/ctf_metric_flags, batch]
      exporters: [debug, otlphttp/grafana_cloud]
    logs:
      # Add host ID first, then apply resource detection and other processors; batch last before export
      processors: [transform/add_host_id, resourcedetection, memory_limiter, transform/sanitize_logs, filter/drop_loadgen, batch]
      exporters: [debug, otlphttp/grafana_cloud]
    profiles:
      # Add host ID first, then apply other processors.
      # Note: the batch processor does not support the profiles signal, so it is omitted here.
      processors: [transform/add_host_id, filter/sanitize_profiles, memory_limiter, filter/drop_loadgen]
      exporters: [debug, otlphttp/grafana_cloud]
